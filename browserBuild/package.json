{
	"name": "browserbuild",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"build": "node flushLib.js && cd .. && npm run build && cd browserBuild && webpack && node wrapperPostProcess.js",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"webpack": "^5.16.0",
		"webpack-cli": "^4.4.0"
	}
}
